<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Flemish Verbs Master</title>
    <meta name="description" content="Learn Flemish verbs with audio and examples">
    <meta name="theme-color" content="#2c3e50">
    <link rel="manifest" href="manifest.json">
    <link rel="icon" href="icons/icon-192x192.png">
    <style>
        :root {
            --bg-primary: #1a1a1a;
            --bg-secondary: #2d2d2d;
            --text-primary: #ffffff;
            --text-secondary: #b0b0b0;
            --accent-color: #3498db;
            --card-bg: #2d2d2d;
            --shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
        }

        .light-mode {
            --bg-primary: #f5f7fa;
            --bg-secondary: #ffffff;
            --text-primary: #2c3e50;
            --text-secondary: #7f8c8d;
            --accent-color: #3498db;
            --card-bg: #ffffff;
            --shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background: var(--bg-primary);
            color: var(--text-primary);
            min-height: 100vh;
            transition: all 0.3s ease;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        header {
            text-align: center;
            margin-bottom: 30px;
            padding: 20px;
            background: var(--bg-secondary);
            border-radius: 15px;
            box-shadow: var(--shadow);
        }

        h1 {
            color: var(--text-primary);
            margin-bottom: 10px;
            font-size: 2.2rem;
        }

        .subtitle {
            color: var(--text-secondary);
            font-size: 1.1rem;
            margin-bottom: 20px;
        }

        .controls {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-bottom: 20px;
            flex-wrap: wrap;
        }

        .btn {
            padding: 10px 20px;
            background: var(--accent-color);
            color: white;
            border: none;
            border-radius: 25px;
            cursor: pointer;
            transition: all 0.3s;
            font-size: 0.9rem;
        }

        .btn:hover {
            opacity: 0.9;
            transform: translateY(-2px);
        }

        .btn-outline {
            background: transparent;
            border: 2px solid var(--accent-color);
            color: var(--accent-color);
        }

        .search-container {
            margin: 20px 0;
            display: flex;
            justify-content: center;
        }

        #search {
            width: 70%;
            padding: 12px 20px;
            background: var(--bg-primary);
            color: var(--text-primary);
            border: 2px solid var(--accent-color);
            border-radius: 30px;
            font-size: 1rem;
            outline: none;
        }

        .verbs-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }

        .verb-card {
            background: var(--card-bg);
            border-radius: 15px;
            padding: 20px;
            box-shadow: var(--shadow);
            transition: transform 0.3s, box-shadow 0.3s;
            cursor: pointer;
        }

        .verb-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
        }

        .verb-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
        }

        .verb-dutch {
            font-size: 1.5rem;
            font-weight: bold;
            color: var(--text-primary);
        }

        .verb-translation {
            font-size: 1.1rem;
            color: var(--text-secondary);
        }

        .verb-example {
            margin-top: 15px;
            padding: 15px;
            background: var(--bg-primary);
            border-radius: 10px;
            font-style: italic;
            color: var(--text-secondary);
            line-height: 1.5;
        }

        .verb-audio {
            margin-top: 15px;
            display: flex;
            align-items: center;
        }

        .audio-btn {
            background: var(--accent-color);
            color: white;
            border: none;
            border-radius: 50%;
            width: 40px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            margin-right: 10px;
            transition: background 0.3s;
        }

        .audio-btn:hover {
            opacity: 0.9;
        }

        .conjugation {
            margin-top: 15px;
            font-size: 0.9rem;
            color: var(--text-secondary);
        }

        .conjugation span {
            display: inline-block;
            margin-right: 8px;
            margin-bottom: 5px;
            background: var(--bg-primary);
            padding: 4px 8px;
            border-radius: 5px;
            font-size: 0.8rem;
        }

        footer {
            text-align: center;
            padding: 30px 20px;
            color: var(--text-secondary);
            font-size: 0.9rem;
            line-height: 1.6;
        }

        .contact-link {
            color: var(--accent-color);
            text-decoration: none;
        }

        .contact-link:hover {
            text-decoration: underline;
        }

        @media (max-width: 768px) {
            .verbs-grid {
                grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            }
            
            #search {
                width: 90%;
            }
            
            .controls {
                flex-direction: column;
                align-items: center;
            }
            
            .btn {
                width: 200px;
            }
        }

        @media (max-width: 480px) {
            .verbs-grid {
                grid-template-columns: 1fr;
            }
            
            h1 {
                font-size: 1.8rem;
            }
        }
    </style>
</head>
<body class="dark-mode">
    <div class="container">
        <header>
            <h1 id="app-title">Flemish Verbs Master</h1>
            <p class="subtitle" id="app-subtitle">Learn 50 essential Flemish verbs with audio and examples</p>
            
            <div class="controls">
                <button class="btn" id="theme-toggle">🌙 Light Mode</button>
                <button class="btn btn-outline" id="language-toggle">🇯🇵 日本語</button>
                <button class="btn" id="install-btn">📱 Install App</button>
            </div>
            
            <div class="search-container">
                <input type="text" id="search" placeholder="Search verbs...">
            </div>
        </header>
        
        <main>
            <div class="verbs-grid" id="verbs-container">
                <!-- Verbs will be loaded here by JavaScript -->
            </div>
        </main>
        
        <footer>
            <p id="contact-text">Contact us<br>
                <a href="mailto:mementomorikatsu@gmail.com" class="contact-link">mementomorikatsu@gmail.com</a>
            </p>
        </footer>
    </div>

    <script>
        // Language texts
        const translations = {
            en: {
                title: "Flemish Verbs Master",
                subtitle: "Learn 50 essential Flemish verbs with audio and examples",
                searchPlaceholder: "Search verbs...",
                themeToggle: "🌙 Light Mode",
                languageToggle: "🇯🇵 日本語",
                installBtn: "📱 Install App",
                contact: "Contact us",
                audioText: "Play audio",
                categories: {
                    all: "All Verbs",
                    common: "Common Verbs",
                    action: "Action Verbs"
                }
            },
            ja: {
                title: "フラマン語動詞マスター",
                subtitle: "音声付きで50の基本フラマン語動詞を学びましょう",
                searchPlaceholder: "動詞を検索...",
                themeToggle: "🌙 ライトモード",
                languageToggle: "🇺🇸 English",
                installBtn: "📱 アプリをインストール",
                contact: "お問い合わせ",
                audioText: "音声を再生",
                categories: {
                    all: "すべての動詞",
                    common: "基本動詞",
                    action: "動作動詞"
                }
            }
        };

        // Verb data
        const verbs = [
            {
                dutch: "zijn",
                translations: {
                    en: "to be",
                    ja: "～である"
                },
                examples: {
                    en: "Ik ben student. (I am a student)",
                    ja: "Ik ben student. (私は学生です)"
                },
                conjugation: ["ben", "bent", "is", "zijn"],
                category: "common"
            },
            {
                dutch: "hebben",
                translations: {
                    en: "to have",
                    ja: "持つ"
                },
                examples: {
                    en: "Ik heb een boek. (I have a book)",
                    ja: "Ik heb een boek. (私は本を持っています)"
                },
                conjugation: ["heb", "hebt", "heeft", "hebben"],
                category: "common"
            },
            {
                dutch: "doen",
                translations: {
                    en: "to do",
                    ja: "する"
                },
                examples: {
                    en: "Wat doe je? (What are you doing?)",
                    ja: "Wat doe je? (何をしていますか?)"
                },
                conjugation: ["doe", "doet", "doet", "doen"],
                category: "common"
            },
            {
                dutch: "zeggen",
                translations: {
                    en: "to say",
                    ja: "言う"
                },
                examples: {
                    en: "Hij zegt de waarheid. (He tells the truth)",
                    ja: "Hij zegt de waarheid. (彼は真実を言います)"
                },
                conjugation: ["zeg", "zegt", "zegt", "zeggen"],
                category: "common"
            },
            {
                dutch: "kunnen",
                translations: {
                    en: "can, to be able to",
                    ja: "～できる"
                },
                examples: {
                    en: "Ik kan zwemmen. (I can swim)",
                    ja: "Ik kan zwemmen. (私は泳ぐことができます)"
                },
                conjugation: ["kan", "kunt", "kan", "kunnen"],
                category: "common"
            },
            {
                dutch: "gaan",
                translations: {
                    en: "to go",
                    ja: "行く"
                },
                examples: {
                    en: "We gaan naar school. (We go to school)",
                    ja: "We gaan naar school. (私たちは学校へ行きます)"
                },
                conjugation: ["ga", "gaat", "gaat", "gaan"],
                category: "action"
            },
            {
                dutch: "zien",
                translations: {
                    en: "to see",
                    ja: "見る"
                },
                examples: {
                    en: "Ik zie een vogel. (I see a bird)",
                    ja: "Ik zie een vogel. (私は鳥を見ます)"
                },
                conjugation: ["zie", "ziet", "ziet", "zien"],
                category: "action"
            },
            {
                dutch: "willen",
                translations: {
                    en: "to want",
                    ja: "～したい"
                },
                examples: {
                    en: "Ik wil koffie drinken. (I want to drink coffee)",
                    ja: "Ik wil koffie drinken. (私はコーヒーが飲みたい)"
                },
                conjugation: ["wil", "wilt", "wil", "willen"],
                category: "common"
            },
            {
                dutch: "komen",
                translations: {
                    en: "to come",
                    ja: "来る"
                },
                examples: {
                    en: "Hij komt uit België. (He comes from Belgium)",
                    ja: "Hij komt uit België. (彼はベルギーから来ました)"
                },
                conjugation: ["kom", "komt", "komt", "komen"],
                category: "action"
            },
            {
                dutch: "moeten",
                translations: {
                    en: "must, to have to",
                    ja: "～しなければならない"
                },
                examples: {
                    en: "Ik moet studeren. (I have to study)",
                    ja: "Ik moet studeren. (私は勉強しなければならない)"
                },
                conjugation: ["moet", "moet", "moet", "moeten"],
                category: "common"
            }
            // Add more verbs here to reach 50
        ];

        let currentLanguage = 'en';
        let currentTheme = 'dark';

        // Initialize app
        function initApp() {
            renderVerbs();
            setupEventListeners();
            registerServiceWorker();
        }

        // Render verbs to the grid
        function renderVerbs(filter = "all") {
            const container = document.getElementById('verbs-container');
            container.innerHTML = '';
            
            const filteredVerbs = filter === "all" ? verbs : verbs.filter(verb => verb.category === filter);
            
            filteredVerbs.forEach(verb => {
                const card = document.createElement('div');
                card.className = 'verb-card';
                card.innerHTML = `
                    <div class="verb-header">
                        <div class="verb-dutch">${verb.dutch}</div>
                        <div class="verb-translation">${verb.translations[currentLanguage]}</div>
                    </div>
                    <div class="verb-example">${verb.examples[currentLanguage]}</div>
                    <div class="conjugation">
                        ${verb.conjugation.map(form => `<span>${form}</span>`).join('')}
                    </div>
                    <div class="verb-audio">
                        <button class="audio-btn" onclick="playAudio('${verb.dutch}')">
                            🔊
                        </button>
                        <span>${translations[currentLanguage].audioText}</span>
                    </div>
                `;
                container.appendChild(card);
            });
        }

        // Play audio using Web Speech API
        function playAudio(word) {
            if ('speechSynthesis' in window) {
                const utterance = new SpeechSynthesisUtterance(word);
                utterance.lang = 'nl-NL';
                utterance.rate = 0.8;
                speechSynthesis.speak(utterance);
            } else {
                alert(`Playing audio for: ${word}`);
            }
        }

        // Toggle theme
        function toggleTheme() {
            const body = document.body;
            const themeToggle = document.getElementById('theme-toggle');
            
            if (currentTheme === 'dark') {
                body.classList.add('light-mode');
                body.classList.remove('dark-mode');
                themeToggle.textContent = currentLanguage === 'en' ? '🌙 Dark Mode' : '🌙 ダークモード';
                currentTheme = 'light';
            } else {
                body.classList.remove('light-mode');
                body.classList.add('dark-mode');
                themeToggle.textContent = currentLanguage === 'en' ? '🌙 Light Mode' : '🌙 ライトモード';
                currentTheme = 'dark';
            }
        }

        // Toggle language
        function toggleLanguage() {
            currentLanguage = currentLanguage === 'en' ? 'ja' : 'en';
            updateTexts();
            renderVerbs();
        }

        // Update all texts based on current language
        function updateTexts() {
            document.getElementById('app-title').textContent = translations[currentLanguage].title;
            document.getElementById('app-subtitle').textContent = translations[currentLanguage].subtitle;
            document.getElementById('search').placeholder = translations[currentLanguage].searchPlaceholder;
            document.getElementById('theme-toggle').textContent = translations[currentLanguage].themeToggle;
            document.getElementById('language-toggle').textContent = translations[currentLanguage].languageToggle;
            document.getElementById('install-btn').textContent = translations[currentLanguage].installBtn;
            document.getElementById('contact-text').innerHTML = `${translations[currentLanguage].contact}<br>
                <a href="mailto:mementomorikatsu@gmail.com" class="contact-link">mementomorikatsu@gmail.com</a>`;
        }

        // Setup event listeners
        function setupEventListeners() {
            // Search functionality
            document.getElementById('search').addEventListener('input', function(e) {
                const searchTerm = e.target.value.toLowerCase();
                const cards = document.querySelectorAll('.verb-card');
                
                cards.forEach(card => {
                    const dutchWord = card.querySelector('.verb-dutch').textContent.toLowerCase();
                    const translation = card.querySelector('.verb-translation').textContent.toLowerCase();
                    
                    if (dutchWord.includes(searchTerm) || translation.includes(searchTerm)) {
                        card.style.display = 'block';
                    } else {
                        card.style.display = 'none';
                    }
                });
            });

            // Theme toggle
            document.getElementById('theme-toggle').addEventListener('click', toggleTheme);

            // Language toggle
            document.getElementById('language-toggle').addEventListener('click', toggleLanguage);

            // Install app
            document.getElementById('install-btn').addEventListener('click', installApp);
        }

        // Install PWA
        let deferredPrompt;
        window.addEventListener('beforeinstallprompt', (e) => {
            e.preventDefault();
            deferredPrompt = e;
        });

        function installApp() {
            if (deferredPrompt) {
                deferredPrompt.prompt();
                deferredPrompt.userChoice.then((choiceResult) => {
                    if (choiceResult.outcome === 'accepted') {
                        console.log('User accepted the install prompt');
                    }
                    deferredPrompt = null;
                });
            }
        }

        // Register service worker
        function registerServiceWorker() {
            if ('serviceWorker' in navigator) {
                navigator.serviceWorker.register('sw.js')
                    .then(registration => console.log('SW registered'))
                    .catch(error => console.log('SW registration failed'));
            }
        }

        // Initialize app when DOM is loaded
        document.addEventListener('DOMContentLoaded', initApp);
    </script>
</body>
</html>
